services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: pcbaudit
      POSTGRES_PASSWORD: localdevpassword
      POSTGRES_DB: pcb_defect_auditor
    ports:
      - "5432:5432"
    volumes:
      - ./sql/schema.sql:/docker-entrypoint-initdb.d/schema.sql